builds:
  - binary: mackerel-plugin-linux-netdev
    goos:
      - linux
    goarch:
      - amd64
      - arm64
archives:
  - format: zip
    name_template: "{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}"
release:
  github:
    owner: monitoring-forge
    name: mackerel-plugin-linux-netdev
